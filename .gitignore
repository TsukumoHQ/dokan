/target
.fastembed_cache

# secrets / env — repo is public, never commit credentials
.env
.env.*
*.pem
*.key
*.p12
secrets*
.config/

# local-only Claude config (personal hooks with machine paths)
.claude/settings.local.json
.claude/hooks/path-guard.sh

# release-gate build input (the just-built binary, copied in by CI)
dokan-gate-bin

install-smoke/__pycache__/
